mode: sidecar # values: daemonset | deployment | statefulset
volumes: {}
volumeMounts: {}
config: {}
  # receivers:
  #   jaeger:
  #     protocols:
  #       thrift_compact:
  # processors:

  # exporters:
  #   debug:

  # service:
  #   pipelines:
  #     traces:
  #       receivers: [jaeger]
  #       processors: []
  #       exporters: [debug]
